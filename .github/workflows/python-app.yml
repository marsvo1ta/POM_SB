
name: Testing NP

on:
  schedule:
    - cron: '00 8 * * 1,3'
  push:
    branches: [ "main" ]
    


permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ hashFiles('setup.py') }}-${{ hashFiles('requirements.txt') }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade --upgrade-strategy eager seleniumbase names pytest-md pytest-github-report
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Test
      run: |
        export NPS_PASS=${{ secrets.PASSWORD }}
        export NPS_EMAIL=${{ secrets.EMAIL }}
        export NPS_URL=${{ secrets.NPS_URL }}
        pytest tests/test_np.py --headless --github-report --dashboard
        
    - name: Send Gmail
      run: |
        python send_to_gmail.py
